# Validation-Contract Audit — full mechanical sweep

Generated by `scripts/hooks/contract-analyzer.js` (deterministic; runtime Zod introspection + route-file pairing).
Unlike the earlier LLM sampling passes, this examines EVERY route.

## Coverage

| | old detector | this analyzer |
|---|---|---|
| schemas resolved | 128 unresolvable | **751 loaded, 0 errors** |
| route/schema/controller triples compared | 81 | **385** |
| findings silently discarded as warnings | 212 | **0** (all reported) |

Routes: 1214 total, 655 mutating, 444 with validateBody. Skipped: handler-not-found=57, schema-not-loaded=2.

## Findings (371 after dedupe)

| type | count | symptom |
|---|---|---|
| SILENT_DROP | 18 | data silently lost on save |
| REQUIRED_UNUSED | 13 | 400 blocks valid input |
| QUERY_COERCION_LOST | 47 | 500 / wrong filter (Express-5 validatedQuery) |
| PASSTHROUGH_WHOLESALE | 84 | mass-assignment (unvalidated keys reach Prisma) |
| NO_VALIDATION | 207 | no body validation at all |
| SCHEMA_UNRESOLVED | 2 | analyzer gap |

## SILENT_DROP — worst class (data lost on save)

- **PUT /:id** — drops `currentBalance`
  - backend/src/controllers/bankAccounts.controller.ts:115 · schema `updateBankAccountSchema (bankAccounts.schema.ts)`
- **POST /:styleId/add-width** — drops `printDirection`
  - backend/src/controllers/cad-planning.controller.ts:1058 · schema `addCADWidthSchema (cadPlanning.schema.ts)`
- **POST /send-out** — drops `sourceFabricStockId`, `sentWidth`, `forStyleId`, `forOrderId`
  - backend/src/controllers/embroidery-stock.controller.ts:9 · schema `sendOutSchema (externalProcess.schema.ts)`
- **POST /receive** — drops `receivedWidth`, `qualityGrade`, `warehouseLocation`
  - backend/src/controllers/embroidery-stock.controller.ts:60 · schema `receiveSchema (externalProcess.schema.ts)`
- **POST /cad** — drops `cutableWidth`, `widthUnit`, `cadMeters`, `cadYards`, `cadWastagePercent`, `markerEfficiency`, `supplierAvailability`, `priceDifferential`, `markerPlanFile`, `markerLengthMeters`, `piecesPerMarker`, `notes`
  - backend/src/controllers/fabric-cad.controller.ts:65 · schema `createCADSchema (fabricGreige.schema.ts)`
- **PUT /cad/:id** — drops `cutableWidth`, `widthUnit`, `cadMeters`, `cadYards`, `cadWastagePercent`, `markerEfficiency`, `supplierAvailability`, `priceDifferential`, `markerPlanFile`, `markerLengthMeters`, `piecesPerMarker`, `notes`
  - backend/src/controllers/fabric-cad.controller.ts:173 · schema `updateCADSchema (fabricGreige.schema.ts)`
- **POST /requirements/:id/allocate-stock** — drops `warehouseId`
  - backend/src/controllers/mrp.controller.ts:154 · schema `allocateStockSchema (saleOrder.schema.ts)`
- **PATCH /requirements/:id/status** — drops `buyerRemarks`, `rejectionReason`, `approvalReference`
  - backend/src/controllers/laceLabDip.controller.ts:115 · schema `updateRequirementStatusSchema (mrp.schema.ts)`
- **POST /thread-requirements/available-suppliers** — drops `requirementIds`
  - backend/src/controllers/order-thread-requirement.controller.ts:172 · schema `availableSuppliersSchema (orderThreadRequirement.schema.ts)`
- **PATCH /:id/select-cad** — drops `recalculateCosting`
  - backend/src/controllers/orderProductionStatus.controller.ts:51 · schema `selectCadSchema (orderItems.schema.ts)`
- **PATCH /:id/inheritance** — drops `inheritStyleSamples`, `inheritInspections`
  - backend/src/controllers/orderProductionStatus.controller.ts:82 · schema `updateInheritanceSchema (orderItems.schema.ts)`
- **POST /:id/recalculate-costing** — drops `selectedCadId`
  - backend/src/controllers/orderProductionStatus.controller.ts:105 · schema `recalculateCostingSchema (orderItems.schema.ts)`
- **POST /:id/rework** — drops `reason`
  - backend/src/controllers/processingStage.controller.ts:105 · schema `markForReworkSchema (processing.schema.ts)`
- **POST /:styleId/images/reorder** — drops `imageIds`
  - backend/src/controllers/style-image.controller.ts:71 · schema `reorderImagesSchema (styleImage.schema.ts)`
- **PATCH /:id/actuals** — drops `fabricActual`, `trimsActual`, `cmtActual`, `embroideryActual`, `accessoriesActual`, `totalActual`
  - backend/src/controllers/style-costing-approval.controller.ts:620 · schema `updateActualsSchema (styleCosting.schema.ts)`
- **POST /variance/:id/approve** — drops `action`, `notes`
  - backend/src/controllers/style-costing-approval.controller.ts:673 · schema `approveVarianceSchema (styleCosting.schema.ts)`
- **POST /:costingId/lace-items/calculate-options** — drops `quantityPerGarment`, `orderQuantity`, `wastagePercent`, `styleId`
  - backend/src/controllers/styleCostingLaceItems.controller.ts:190 · schema `calculateLaceOptionsSchema (styleCosting.schema.ts)`
- **POST /convert** — drops `ply`, `packagingType`, `inputType`, `value`
  - backend/src/controllers/thread-conversion.controller.ts:8 · schema `convertThreadSchema (trimMasters.schema.ts)`

### Hand-verified examples
- `POST /cad` (fabric CAD): validates against `createCADSchema` (greigeWidth/cadValue/shrinkage) while the controller reads cutableWidth/cadMeters/markerEfficiency/… — **12 of 13 fields discarded, only fabricId survives**.
- `PATCH /:id/actuals` (cost sheet): schema expects actualFabricCost/actualTrimCost/… controller reads fabricActual/trimsActual/cmtActual/… — **zero overlap, all 6 money fields discarded**.
- `POST /:costingId/lace-items/calculate-options`: known-live, grandfathered in schema-alignment-baseline.json.
